paths:
  migrations: 'migrations'
templates:
  file: migrations/stubs/create.stub
environments:
  default_migration_table: phinxlog
  default_environment: development
  development:
    adapter: mysql
    host: 127.0.0.1
    name: school-boards
    user: root
    pass: ''
    port: 3306
    charset: utf8

version_order: creation
